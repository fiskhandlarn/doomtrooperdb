{{ deck.name }} {{ deck.version }}

{% set first_expansion = deck.included_expansions|first %}
{% set last_expansion = deck.included_expansions|last %}
Expansions: {% if deck.included_expansions|length > 1 %}
From {{ first_expansion.expansion.name }}{% if first_expansion.nb > 1 %} ({{ first_expansion.nb }}){% endif %} to {{ last_expansion.expansion.name }}{% if last_expansion.nb > 1 %} ({{ last_expansion.nb }}){% endif %}
{% else %}
{{ first_expansion.expansion.name }}{% if first_expansion.nb > 1 %} ({{ first_expansion.nb }}){% endif %}
{% endif %}


{% set alliance = deck.slots_by_type.alliance %}
Alliance{% if alliance|length > 1 %}s{% endif %}

{% for slot in alliance %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set art = deck.slots_by_type.art %}
Art{% if art|length > 1 %}s{% endif %}

{% for slot in art %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set beast = deck.slots_by_type.beast %}
Beast{% if beast|length > 1 %}s{% endif %}

{% for slot in beast %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set symmetry = deck.slots_by_type.symmetry %}
Dark Symmetry{% if symmetry|length > 1 %}s{% endif %}

{% for slot in symmetry %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set equipment = deck.slots_by_type.equipment %}
Equipment{% if equipment|length > 1 %}s{% endif %}

{% for slot in equipment %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set ki = deck.slots_by_type.ki %}
Ki Power{% if ki|length > 1 %}s{% endif %}

{% for slot in ki %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set mission = deck.slots_by_type.mission %}
Mission{% if mission|length > 1 %}s{% endif %}

{% for slot in mission %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set relic = deck.slots_by_type.relic %}
Relic{% if relic|length > 1 %}s{% endif %}

{% for slot in relic %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set special = deck.slots_by_type.special %}
Special{% if special|length > 1 %}s{% endif %}

{% for slot in special %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set warrior = deck.slots_by_type.warrior %}
Warrior{% if warrior|length > 1 %}s{% endif %}

{% for slot in warrior %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}

{% set warzone = deck.slots_by_type.warzone %}
Warzone{% if warzone|length > 1 %}s{% endif %}

{% for slot in warzone %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.expansion.name }})
{% endfor %}
